workflows:
  android-release:
    name: Android Build
    environment:
      node: 20
      java: 17
    scripts:
      - npm install
      - npm run build
      - npx cap add android
      - npx cap copy
      - cd android && ./gradlew assembleDebug
    artifacts:
      - android/app/build/outputs/**/*.apk
